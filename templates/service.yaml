apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-chart.fullname" . }}-service
  labels:
    {{- include "web-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "web-chart.selectorLabels" . | nindent 4 }}
